<form class="feuille_raoul" autocomplete="off">
    <header class="sheet-header">
        <img src="{{item.img}}" class="imgHero" data-edit="img" title="{{item.name}}" width="128" height="128"/les> <h1>{{item.name}}</h1> 
    </header> 
 <div>
     <h3>Description</h3>
<textarea name="system.description" rows="5">{{item.system.description}}</textarea>
</div>
</form>
